---
aliases:
  - 20260828-dev-plan
tags:
  - plan
  - dev
  - phase-0
  - frontend-skeleton
created: 2026-08-28
agent: 20-经济-Economy
trigger: 每日 03:10 cron T4
---

# 2026-08-28 · 当日开发计划(frontend 最小骨架)

> 触发:每日 03:10 cron T4(系统本次调度时无昨日 8-27 T4 plan,基于 `Logs/巡检-经济-20260828.md` 硬性目标决策)
> 上游决策依据:`Logs/巡检-经济-20260828.md` §"T4 今日硬性目标"建议"frontend 最小骨架"
> 为什么不走 akshare 真实拉取:8-28 巡检 §巡检项 1 明确 Python 3.9.6 + pip 21.2 环境未升级,显式环境升级不在 T4 自然节奏内
> 为什么不走 §5-3 政策文件库:本任务定位"1 个小变更",policy OCR + 结构化是 ≥3 天工作量
> 为什么不走 §5-6 归因 schema 设计:归因 schema 需要指标库先有数据,frontend 不阻塞后续

---

## §0 今日唯一产出

**`frontend/` 最小骨架(Next.js 14 App Router)**,与已有 `backend/ingest/` 形成最小可运行栈:

- 读 SQLite:`frontend/app/api/indicators/route.ts` → 直接 better-sqlite3 读 `../data/economy_ingest.db`
- 渲染 5 指标卡片:`frontend/app/page.tsx`(Server Component)
- 不引入 UI 库,纯 CSS module 卡片,保持简洁可入库

## §1 任务清单(本日完成)

1. [x] `frontend/package.json` — Next.js 14 + React 18 + better-sqlite3 + TypeScript
2. [x] `frontend/tsconfig.json` — Next.js 标准配置
3. [x] `frontend/next.config.js` — 基础配置(serverComponentsExternalPackages 含 better-sqlite3)
4. [x] `frontend/.gitignore` — node_modules / .next / 锁文件保留策略
5. [x] `frontend/app/layout.tsx` — 根布局(标题 + 中文 locale)
6. [x] `frontend/app/globals.css` — 极简样式(卡片 + 5 指标栅格)
7. [x] `frontend/app/page.tsx` — 主页:Server Component,读 5 指标 + 已落库条数,渲染卡片
8. [x] `frontend/app/api/indicators/route.ts` — REST 路由:返回 5 指标 JSON
9. [x] `frontend/lib/db.ts` — better-sqlite3 单例 + 复用 INDICATORS 字典
10. [x] `frontend/README.md` — 使用说明 + §5-2 Day 4 frontend 起步标记
11. [x] `项目开发计划.md §5-2` — 新增子项 "frontend 最小骨架(2026-08-28)",勾选 [x]
12. [x] `README.md` — 项目结构图加 `frontend/`,新增"Phase 1 MVP 启动基线 v2"小节

## §2 不做(留给后续 T4)

- ❌ npm install + npm run dev 验证(本机虽装 node 24 / npm 11,但 better-sqlite3 需要重编译,不在 T4 自然节奏内)
- ❌ LLM 解读卡片(等 Phase 1 MVP §6 第 1 项飞书 Agent 上线对接)
- ❌ 归因引擎(等 §5-6 归因 schema v0.1)
- ❌ ClickHouse 时序库对接(等 §5-2 Day 5)

## §3 验收标准

- `frontend/package.json` 含 next@14 / react@18 / better-sqlite3 / typescript
- `frontend/app/page.tsx` 读 SQLite `indicator` + `indicator_value`,展示 5 指标卡片(代码名 / 中文名 / 来源 / 频率 / 单位 / 优先级 / 已落库条数 / 最新期)
- `frontend/app/api/indicators/route.ts` 返回 `[{ code, name_cn, source, freq, unit, priority, last_seen, value_count }]`
- `frontend/README.md` 含本地跑通步骤 + 与 `backend/ingest/README.md` 的接口契约
- `项目开发计划.md §5-2` 第 4 子项 frontend 最小骨架(2026-08-28)标 [x]
- 根 `README.md` 项目结构图加 `frontend/`,并新增 "Phase 1 MVP 启动基线 v2" 小节

## §4 关联文档

- `Logs/巡检-经济-20260828.md` §"T4 今日硬性目标"上游决策
- `项目开发计划.md §5-2` 跑通 5 大免费宏观数据源(本任务作为子项)
- `项目开发计划.md §6 Phase 1 MVP` 第 3 项 Web 看板(本任务为前置依赖)
- `backend/ingest/README.md` EconomyIngest schema 数据契约(供 frontend 复用)
- `.plan/20260824-inspiration-index.md` §1 IND-01 指标库底座(本任务为可视化出口)

## §5 变更记录

| 时间 | 变更 | 触发 |
|---|---|---|
| 2026-08-28 03:10 | 本文件首次生成:T4 决策依据缺失,基于 8-28 巡检硬性目标补 plan | 每日 03:10 cron T4(本次补 plan) |
